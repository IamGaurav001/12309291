{
  "name": "gaurav-assignment-root",
  "version": "1.0.0",
  "description": "My assignment project - root folder to run everything",
  "scripts": {
    "start": "cd vehicle_maintenance_scheduler && npm start",
    "dev": "cd vehicle_maintenance_scheduler && npm run dev",
    "priority-inbox": "node priority_inbox.js"
  }
}
